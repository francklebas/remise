project_id = "remise"

[auth]
site_url = "https://boardly.francklebas.com"
additional_redirect_urls = ["https://boardly.francklebas.com", "http://localhost:5173"]

[auth.email]
enable_confirmations = true

# Supabase CLI runs Mailpit as part of the local Docker stack. These are the
# ports exposed on the host; Auth reaches Mailpit through Docker's network.
[local_smtp]
enabled = true
port = 8025
smtp_port = 1025
